## What / Why

<!-- What this PR does and why. Link the issue if there is one. -->

## Contract change?

<!-- Does this change any REST route, WS protocol, or payload shape?
     If yes: docs/api.md must be updated in a dedicated commit *before* the
     code changes (contract-first — see CONTRIBUTING.md). If no: say "No". -->

## How verified

- [ ] `make test`
- [ ] `make lint`
- [ ] `make build`
- [ ] `make e2e` (only if the change touches real training/inference paths; needs Apple Silicon)

<!-- Anything you checked manually (e.g. via `make dev` / `make run`). -->
